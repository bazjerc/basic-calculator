{
  "name": "calculator",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sassw": "sass --watch ./sass/index.scss:./inndex.css --no-source-map",
    "bsync": "browser-sync start --server --files '*.html' '*.css' '*.js' --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "sass": "^1.55.0"
  }
}
